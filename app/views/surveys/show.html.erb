<p style="color: green"><%= notice %></p>

<%= render @survey %>

<div class="d-flex justify-content-center mt-2">
  <%= link_to "Edit", edit_survey_path(@survey), class: "btn btn-warning mx-1" %>
  <%= link_to "Back to Surveys", surveys_path, class: "btn btn-primary mx-3" %>
  <%= button_to "Delete", @survey, method: :delete, data: {turbo_confirm: "Are you sure you want to delete?" }, class: "btn btn-danger" %>
</div>
